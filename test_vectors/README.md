Test vectors are small JSON instances that should validate (minimal/) or intentionally fail (negative/) against the schemas.

Negative vectors are designed to enforce the constitution:
- “answer-like fields” must be rejected.
- missing provenance in exported bundles must be rejected.
